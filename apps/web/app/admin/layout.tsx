import '../estilos/admin/admin-panel.css';

export const metadata = {
  title: 'Admin — Financieramente',
  description: 'Centro de comando administrativo',
};

export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return children;
}
